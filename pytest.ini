[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    db: needs a database (DATABASE_URL or the embedded cluster in PG_DATA_DIR) seeded with scripts.seed_mock
filterwarnings =
    ignore::DeprecationWarning
